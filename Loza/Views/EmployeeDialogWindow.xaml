<Window x:Class="Lota.Views.EmployeeDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Сотрудник" Height="470" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="lblTitle" Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,15"/>

        <TextBox Grid.Row="1" x:Name="txtLastName" Margin="0,5"
                 materialDesign:HintAssist.Hint="Фамилия"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <TextBox Grid.Row="2" x:Name="txtFirstName" Margin="0,5"
                 materialDesign:HintAssist.Hint="Имя"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <TextBox Grid.Row="3" x:Name="txtMiddleName" Margin="0,5"
                 materialDesign:HintAssist.Hint="Отчество (необязательно)"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <TextBox Grid.Row="4" x:Name="txtPosition" Margin="0,5"
                 materialDesign:HintAssist.Hint="Должность"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <TextBox Grid.Row="5" x:Name="txtDepartment" Margin="0,5"
                 materialDesign:HintAssist.Hint="Отдел"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
        <DatePicker Grid.Row="6" x:Name="dpHireDate"
                    materialDesign:HintAssist.Hint="Дата приёма на работу"
                    Style="{StaticResource MaterialDesignDatePicker}"/>
        <ComboBox Grid.Row="7" x:Name="cmbStatus"
                  materialDesign:HintAssist.Hint="Статус"
                  Style="{StaticResource MaterialDesignFloatingHintComboBox}">
            <ComboBoxItem Content="Активный" Tag="active"/>
            <ComboBoxItem Content="Уволен" Tag="fired"/>
        </ComboBox>

        <StackPanel Grid.Row="9" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="Сохранить" Style="{StaticResource MaterialDesignFlatDarkBgButton}"
                    IsDefault="True"
                    Click="BtnSave_Click" Margin="0,0,10,0"/>
            <Button Content="Отмена" Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>