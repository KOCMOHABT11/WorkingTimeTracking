<Window x:Class="Lota.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Учет рабочего времени «Лоза»" Height="450" Width="380"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Вход в систему" Grid.Row="0" 
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Логин -->
        <TextBox Grid.Row="1" x:Name="txtUsername" Margin="0,10"
                 materialDesign:HintAssist.Hint="Имя пользователя"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                 VerticalAlignment="Top" />

        <!-- Пароль (с глазом) -->
        <Grid Grid.Row="2" Margin="0,10">
            <PasswordBox x:Name="txtPasswordHidden"
                 materialDesign:HintAssist.Hint="Пароль"
                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                 PasswordChanged="PasswordHidden_PasswordChanged"
                 Visibility="Visible" />
            <TextBox x:Name="txtPasswordVisible"
             materialDesign:HintAssist.Hint="Пароль"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
             Visibility="Collapsed"
             TextChanged="PasswordVisible_TextChanged" />
            <Button x:Name="btnTogglePassword"
        Width="36" Height="36"
        HorizontalAlignment="Right"
        VerticalAlignment="Center"
        Margin="0,0,8,0"
        Click="BtnTogglePassword_Click"
        Cursor="Hand"
        ToolTip="Показать пароль"
        Style="{x:Null}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" CornerRadius="18" BorderThickness="0">
                            <TextBlock x:Name="eyeText" Text="👁" 
                           FontFamily="Segoe UI Emoji" FontSize="18"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Ошибка -->
        <TextBlock Grid.Row="3" x:Name="txtError" Foreground="{StaticResource MaterialDesignValidationErrorBrush}" 
                   TextWrapping="Wrap" Margin="0,5,0,0" VerticalAlignment="Top" />

        <!-- Кнопка Войти -->
        <Button Grid.Row="4" Content="ВОЙТИ" Margin="0,20,0,10" Height="40"
                Style="{StaticResource MaterialDesignFlatDarkBgButton}"
                Click="BtnLogin_Click" />

        <!-- Переход на регистрацию -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Text="Нет аккаунта? " VerticalAlignment="Center"/>
            <Button Content="Зарегистрироваться" Style="{StaticResource MaterialDesignFlatButton}"
                    Click="BtnGoToRegister_Click" />
        </StackPanel>
    </Grid>
</Window>